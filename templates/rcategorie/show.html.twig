{% extends 'base.html.twig' %}

{% block title %}Rcategorie{% endblock %}

{% block body %}
    <h1>Rcategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rcategorie.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rcategorie_index') }}">back to list</a>

    <a href="{{ path('app_rcategorie_edit', {'id': rcategorie.id}) }}">edit</a>

    {{ include('rcategorie/_delete_form.html.twig') }}
{% endblock %}
